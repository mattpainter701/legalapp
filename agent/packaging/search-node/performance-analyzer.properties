# Performance Analyzer has no client/server authentication. Prefer uninstalling
# the plugin when it is not needed. If retained, its separate listener must stay
# on loopback; this file belongs under config/opensearch-performance-analyzer/.
webservice-bind-host = 127.0.0.1
webservice-listener-port = 9600
https-enabled = false
