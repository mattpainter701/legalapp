name: Deploy IONOS candidate

run-name: ${{ inputs.operation }} IONOS main @ ${{ github.sha }}

on:
  workflow_dispatch:
    inputs:
      operation:
        description: Verify or stage the IONOS production stack
        required: true
        default: verify
        type: choice
        options:
          - verify
          - stage
      confirmation:
        description: Type the operation-specific confirmation shown in the runbook
        required: false
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: law-hand-ionos-production
  cancel-in-progress: false

jobs:
  release-gate:
    name: Validate IONOS release
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      release_sha: ${{ steps.release.outputs.release_sha }}
    steps:
      - name: Require main and explicit mutation confirmation
        env:
          RELEASE_REF: ${{ github.ref }}
          OPERATION: ${{ inputs.operation }}
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          set -Eeuo pipefail
          [[ "$RELEASE_REF" == refs/heads/main ]] || {
            echo "::error::IONOS runs must be dispatched from main"
            exit 1
          }
          case "$OPERATION" in
            verify) ;;
            stage) [[ "$CONFIRMATION" == STAGE-IONOS-CANDIDATE ]] ;;
            *) exit 2 ;;
          esac

      - name: Pin the exact release commit
        id: release
        env:
          RELEASE_SHA: ${{ github.sha }}
        run: |
          [[ "$RELEASE_SHA" =~ ^[0-9a-f]{40}$ ]]
          echo "release_sha=$RELEASE_SHA" >> "$GITHUB_OUTPUT"

      - name: Checkout release evidence helper on GitHub-hosted runner
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v5.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Require successful CI and CodeQL for mutation
        if: inputs.operation == 'stage'
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_SHA: ${{ github.sha }}
        run: |
          python scripts/release_evidence.py --sha "$RELEASE_SHA" \
            --workflow ci.yml --workflow codeql.yml

      - name: Require successful QA acceptance when enabled
        if: inputs.operation == 'stage' && vars.LAWHAND_QA_GATE_REQUIRED == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_SHA: ${{ github.sha }}
        run: |
          python scripts/release_evidence.py --sha "$RELEASE_SHA" \
            --workflow qa-acceptance.yml --event workflow_dispatch

  ionos:
    name: ${{ inputs.operation }} on IONOS Cube M
    needs: release-gate
    runs-on: [self-hosted, Linux, X64, ionos, lawhand-prod]
    environment:
      name: ionos-production
    timeout-minutes: 60
    steps:
      - name: Preflight fixed root-owned entrypoint
        run: |
          set -Eeuo pipefail
          entrypoint=/usr/local/sbin/lawhand-ionos-deploy-from-github
          test -f "$entrypoint" && test -x "$entrypoint"
          stat -c '%U:%G %a' "$entrypoint" | grep -Fqx 'root:root 755'
          grep -Fqx '  verify|stage|deploy|accept) ;;' "$entrypoint"

      - name: Run pinned IONOS operation
        env:
          DEPLOY_OPERATION: ${{ inputs.operation }}
          RELEASE_SHA: ${{ needs.release-gate.outputs.release_sha }}
        run: |
          sudo -n /usr/local/sbin/lawhand-ionos-deploy-from-github \
            "$DEPLOY_OPERATION" "$RELEASE_SHA"

      - name: Record IONOS summary
        if: always()
        env:
          DEPLOY_OPERATION: ${{ inputs.operation }}
          RELEASE_SHA: ${{ needs.release-gate.outputs.release_sha }}
        run: |
          {
            echo "## LawHand IONOS candidate"
            echo
            echo "- Operation: \`$DEPLOY_OPERATION\`"
            echo "- Commit: \`$RELEASE_SHA\`"
            echo "- Runner: \`$RUNNER_NAME\`"
            echo "- Warning: the current stage operation recreates the public production stack; it is not zero-downtime."
          } >> "$GITHUB_STEP_SUMMARY"
