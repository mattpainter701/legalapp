{
  "name": "lawhand-word-addin",
  "version": "0.1.0",
  "description": "LawHand matter intelligence and drafting workspace for Microsoft Word",
  "scripts": {
    "start": "npx http-server . -p 3001 --ssl --cert localhost.crt --key localhost.key"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
