{
  "name": "lawhand-office-addin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3001",
    "build": "tsc --noEmit && vite build && npm run manifests",
    "manifests": "node scripts/build-manifests.mjs",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.0.0"
  },
  "devDependencies": {
    "@types/office-js": "^1.0.585",
    "rollup": "npm:@rollup/wasm-node@4.62.3",
    "typescript": "^5.8.3",
    "vite": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
